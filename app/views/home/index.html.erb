      <%= image_tag('print.png' , class:"absolute bottom-0 right-0 lg:left-0 mx-auto h-5/6 object-cover") %>
      <div
        class="hidden lg:block absolute -bottom-1/4 right-0 left-0 mx-auto w-big h-big bg-indigo-900 rounded-full -z-10"
      ></div>
      <div
        class="absolute top-1/2 left-5 text-xl sm:left-10 sm:text-4xl md:left-1/4 md:text-6xl lg:left-5 xl:left-48 xl:text-7xl font-bold"
      >
        <span class="dark:text-blue-200 text-blue-700">3D PRINTER</span>
        <p id="text" class="text-red-500"></p>
      </div>
      <div
        class="hidden lg:flex flex-col gap-5 rounded-md shadow-lg absolute top-0 bottom-0 m-auto right-10 bg-white dark:bg-slate-900 dark:shadow-slate-800 p-6 h-fit w-1/3"
      >
        <% if user_signed_in?  %>
        <h1 class="text-4xl font-bold text-indigo-900">Welcome, <%= current_user.username %> to Print.io!</h1>
        <p class="text-gray-400">
          This is the blog where you can see all the articles
        </p>
        <% else %>
        <h1 class="text-4xl font-bold text-indigo-900">Welcome to Print.io!</h1>
        <p class="text-gray-400">
          This is the blog where you can see all the articles
        </p>
        <% end %>

      </div>
    </div>
